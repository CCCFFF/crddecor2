<%= form_for(@space_vote) do |f| %>
  <% if @space_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@space_vote.errors.count, "error") %> prohibited this space_vote from being saved:</h2>

      <ul>
      <% @space_vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :space_id %><br>
    <%= f.number_field :space_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
