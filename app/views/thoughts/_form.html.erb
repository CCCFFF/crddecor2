<%= form_for(@thought) do |f| %>
  <% if @thought.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thought.errors.count, "error") %> prohibited this thought from being saved:</h2>

      <ul>
      <% @thought.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
    <%#= f.hidden_field :user_id, :value => current_user.id %>
  <div class="field">
    <%= f.label :space_picture_id %><br>
    <%= f.collection_select :space_picture_id, SpacePicture.all.order("name ASC"), :id, :name, :prompt => "Select a Picture" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
