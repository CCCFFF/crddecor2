          <li id="thought_<%= thought.id %>" class="the_thought_pic_<%= thought.space_picture_id %>" class="media-list">
            <%= thought.text %><br>
            <% user = User.find_by(:id => thought.user_id) %>
            <%= "Commenter: #{user.first_name} #{user.last_name}" %>
            <%= link_to '<i class="icon-trash"></i>'.html_safe, thought, method: :delete, remote: true %>
          </li>
